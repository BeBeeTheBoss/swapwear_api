meta {
  name: Get
  type: http
  seq: 1
}

get {
  url: {{url}}/orders
  body: json
  auth: bearer
}

headers {
  Accept: application/json
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    
  }
}
